# 抖音直播大屏分析飞书机器人 - 依赖列表
# Douyin Live Dashboard Analysis Feishu Bot - Requirements

flask>=2.0.0
flask-cors>=3.0.0
requests>=2.28.0
gunicorn>=20.0.0
# 抖音直播大屏分析飞书机器人 - 依赖列表
# Douyin Live Dashboard Analysis Feishu Bot - Requirements

flask>=2.0.0
flask-cors>=3.0.0
requests>=2.28.0
gunicorn>=20.0.0

# 飞书SDK（可选，如需使用官方SDK）
# lark-oapi>=1.0.0

# Python版本要求
python >= 3.8# 飞书SDK（可选，如需使用官方SDK）
# lark-oapi>=1.0.0

# Python版本要求
python >= 3.8
